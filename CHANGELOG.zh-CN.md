# 本版更新

- Codex Usage Tray Lite 首次公开发布，从 v0.2.16 开始；不发布更早的本地版本和 Git 历史。
- 采用 GPL-3.0-only 发布，保留原有 MIT 归属声明及随包依赖的许可声明。
- 包含 WebView2 和 Codex CLI 两种数据源、四种托盘图标样式、浅色／深色／跟随系统主题，以及五种界面语言。
- 并排双条模式在明确确认账户没有适用的 5 小时额度时，使用完整内框显示每周额度。双额度账户仍保持左侧每周、右侧 5 小时；不明确的数据继续显示未知。
- 每周额度保持独立的绿／橙／红阈值。仅每周额度的回退状态使用中性外框；提示文本、已保存偏好和刷新行为保持不变。
- v0.2.16 本地构建通过 88 项自动化测试，包括 0／1／20／21／50／51／100%、16／20／24／32 像素及浅深色主题下的回退像素检查。这些是自动化检查，不等同于完整的真实浏览器或长时间验收测试。

**安装：**下载 Windows x64 ZIP 及对应校验文件，核对哈希后将全部文件解压到固定目录，运行 `CodexUsageTrayLite.exe`。需要 Windows 11 x64 和 .NET Framework 4.8 或更高版本。WebView2 模式需要安装 WebView2 Evergreen Runtime；CLI 模式使用已安装且通过 ChatGPT 登录的 Codex CLI。

**从本地版本升级：**替换全部程序文件前先选择退出。启用开机启动时保持安装目录不变，或移动后重新启用该设置。`%LOCALAPPDATA%\CodexUsageTrayLite` 中的设置和 WebView2 会话会保留。
