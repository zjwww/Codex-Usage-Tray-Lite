# 0.2.18 本版更新

- 后续本地候选包和 GitHub Release 压缩包统一使用不含 `local` 的 `CodexUsageTrayLite-v<版本>-win-x64.zip`；既有历史包不会改名或覆盖。
- 便携 ZIP 现在同时包含英文 `README.txt` 和完整的简体中文 `README.zh-CN.txt`。
- ZIP 内继续保留完整的中英文更新历史：`0.2.15` 及更早版本保留历史 `-local` 后缀，`0.2.16` 及以后使用公开版本号。
- 包内源码链接会自动跟随当前版本。打包流程会拒绝过期的当前版本标题、缺失的历史分界、未替换的模板变量、错误的源码标签，以及名称中含 `local` 的新压缩包。
- 程序运行行为与 `0.2.17` 相同。

## 包含的 0.2.17 程序更新

- WebView2 与 Codex CLI 两种模式的托盘一级菜单均精简为 15 行。邮箱和账号等级合并到第一行，用量及更新时间仍分别完整显示。
- WebView2 代理／会话维护项移入“用量来源”，CLI 模式会完整隐藏；日志、配置文件夹、帮助和关于统一收进“工具与帮助”。
- “用量来源”“刷新间隔”“图标样式”“主题”“语言”会在右侧显示当前值。菜单仍保持最多两级，立即刷新及登录／帮助入口仍在一级。
- 浅色和深色主题中的主要只读用量信息改为高对比颜色，重置次数和更新时间保持次级灰色。正式 WinForms 渲染检查同时覆盖长邮箱与 150% 字体。
- 五种界面语言中的 `Both (Default)` 显示名称改为 `Stacked (Default)`；仅修改文案，不影响保存设置或图标渲染。
- v0.2.18 构建重新通过全部 89 项程序自动化测试；对于未改变的程序行为，仍检查英语／简体中文浅色和深色菜单、来源／工具／样式子菜单、CLI 显隐、可键盘操作的原生菜单结构及长文字布局。渲染器截图不等同于真实账号刷新或长时间测试。

**安装：**下载 Windows x64 ZIP 及对应校验文件，核对哈希后将全部文件解压到固定目录，运行 `CodexUsageTrayLite.exe`。需要 Windows 11 x64 和 .NET Framework 4.8 或更高版本。WebView2 模式需要安装 WebView2 Evergreen Runtime；CLI 模式使用已安装且通过 ChatGPT 登录的 Codex CLI。

**升级：**替换全部程序文件前先选择退出。启用开机启动时保持安装目录不变，或移动后重新启用该设置。`%LOCALAPPDATA%\CodexUsageTrayLite` 中的设置和 WebView2 会话会保留。
