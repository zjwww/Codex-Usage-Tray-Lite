# 0.3.1 本版更新

- 本地候选包的 ZIP 文件名现在带 `-local`；获准发布的 GitHub 正式包单独生成，包含审核后的双语文档。历史更新日志标题已按实际发布记录区分公开版本与本地迭代。

- 每次刷新成功后会写入一条结构化 `Refresh.Success` 日志，包含所选数据源、脱敏邮箱、用户等级、5-Hour／Weekly 数值和重置时间、可用用量重置次数、获取时间及耗时。缺失值会明确标记，绝不写入未脱敏邮箱。
- 新增仅输出英文的命令行界面，可查看帮助／版本／上次保存状态、控制已运行托盘实例刷新或退出，以及输出本地日志／配置路径。
- `-refresh` 与 `-exit`／`-quit` 使用两个轻量级会话内信号，不会创建第二个托盘、WebView2 控制器或 Codex CLI 采集进程。
- 保留原有内部 `--startup` 参数，使当前用户的“开机启动”继续进入正常托盘启动流程。
- 优化直接交互式命令提示符与 PowerShell 输出：原位清除未变化的提前提示符；仅在 CMD 生成的上一完整可见行为空白时复用该间隔行，使结果紧接命令显示；结果与恢复后的提示符之间保留一个空行，并且不注入任何按键。
- 如果提示符文字或光标位置发生变化，或控制台形式不受支持，会自动保留 v0.2.30 的安全换行布局；正常托盘及开机启动仍不会闪出控制台。
- 新增命令解析、已保存状态、隐私、命名通道、编译后 EXE 输出／退出代码、提示符文字／光标／输出行安全及启动参数兼容覆盖。v0.3.1 构建通过全部 97 项自动化测试。

**安装：**下载 Windows x64 ZIP 及对应校验文件，核对哈希后将全部文件解压到固定目录，运行 `CodexUsageTrayLite.exe`。需要 Windows 11 x64 和 .NET Framework 4.8 或更高版本。WebView2 模式需要安装 WebView2 Evergreen Runtime；CLI 模式使用已安装且通过 ChatGPT 登录的 Codex CLI。

**升级：**替换全部程序文件前先选择退出。启用开机启动时保持安装目录不变，或移动后重新启用该设置。`%LOCALAPPDATA%\CodexUsageTrayLite` 中的设置和 WebView2 会话会保留。
